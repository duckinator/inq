#!/usr/bin/env ruby

require "how_is"
require "how_is/cli"
require "how_is/cli/parser"

options = HowIs::CLI::Parser.new.parse(ARGV)

if options[:use_config_file]
  HowIs::CLI.new.from_config_file(options[:config_file])
else
  HowIs.generate_report_file(options)
end
